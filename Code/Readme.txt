在Matlab中调用方式如下：

拍摄的图像：img

output = Seg(img);

其中output是在原图img上标记程序所分割的细胞（红色表示）的图像。
对于您关心的面积，在程序中有个变量cell_area，里面保存了识别的细胞的数目和面积。

下一步处理：
（1）在识别的细胞的细胞上标记1，2，3，。。。
（2）将细胞面积写入到excel中。

这样，您可以看分割的最终结果，选择想要的细胞，然后再excel中删除识别错误的细胞。剩下的就是您之后的数据分析。

您先看一下，现在的分割结果是否满足您的要求。下一步处理的代码可以独立出来，只需要保存分割的中间结果（例如细胞面积，分割区域等）。
